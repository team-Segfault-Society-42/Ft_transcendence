# ── Docker Networks ──────────────────────────────────────────────────────────
networks:
  client-side:
    driver:
      bridge
  server-side:
    driver:
      bridge

services:
  # ── Frontend (React) ───────────────────────────────────────────────────────
  frontend:
    container_name: react
    image: react
    build:
      context: ./Frontend
      dockerfile: Dockerfile
    networks:
      - client-side
    # Port set to default Vite Port (can be changed)
    ports:
      - "5173:5173"
  
  # ── Backend (Nest.js) ──────────────────────────────────────────────────────
  backend:
    container_name: nestJS
    image: nestJS
    build:
      context: ./Backend
      dockerfile: Dockerfile
    networks:
      - client-side
      - server-side
    ports:
      - "3000:3000"
  
  # ── Reverse Proxy (Nginx) ──────────────────────────────────────────────────
  proxy:
    container_name: nginx
    image: nginx
    build:
      context: ./Proxy
      dockerfile: Dockerfile
    networks:
      - client-side
    ports:
      - "443:443"
    depends_on:
      - frontend
      - backend
  
  # ── Database (Postgres) ────────────────────────────────────────────────────
  # db:
  #   container_name: postgres
  #   image: postgres:alpine
  #   networks:
  #     - server-side
  #   ports:
  #     - "5432:5432"